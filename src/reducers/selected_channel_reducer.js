const selectedChannelReducer = (state, action) => {
  if (state === undefined) {
    return null;
  }
  switch (action.type) {
    default:
      return state;
  }
}

export default selectedChannelReducer;
